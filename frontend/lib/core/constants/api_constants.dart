class ApiConstants {
  ApiConstants._();

  static const String baseUrl = 'https://mine-wadhwani.onrender.com';
  static const Duration connectTimeout = Duration(seconds: 30);
  static const Duration receiveTimeout = Duration(seconds: 30);
}
